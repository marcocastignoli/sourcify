version: "3.8"

services:
  fourbytes-db:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: "fourbytes_test_db"
      POSTGRES_USER: "fourbytes"
      POSTGRES_PASSWORD: "fourbytes"
    ports:
      - ${DOCKER_HOST_POSTGRES_TEST_PORT:-5433}:5432
